{
  "auto_grade": {
    "patch_diff_min_bytes": 60,
    "patch_diff_max_bytes": 800,
    "files_changed_required": [".pre-commit-config.yaml"],
    "metadata_keys_required": ["fixture", "start_time", "end_time", "model", "plugins"]
  },
  "rubric": {
    "llm_judge_prompt": "Verify .pre-commit-config.yaml now contains a check-json hook with id 'check-json' after the trailing-whitespace hook."
  }
}
