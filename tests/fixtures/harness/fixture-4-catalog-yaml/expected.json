{
  "auto_grade": {
    "patch_diff_min_bytes": 200,
    "patch_diff_max_bytes": 2000,
    "files_changed_required": ["catalog/catalog.yaml"],
    "metadata_keys_required": ["fixture", "start_time", "end_time", "model", "plugins"]
  },
  "rubric": {
    "llm_judge_prompt": "Verify catalog/catalog.yaml now contains a 'fixture-demo-plugin' entry appended to plugins: with the exact fields name, category, tags, source, components, and items specified in task.md."
  }
}
