# Generated by scripts/lib/render_seed.py — do not edit by hand.
# Edit templates/seeds/llama-builds.yaml.j2 instead.
schema_version: 1
repo: Heretek-AI/llama-builds
project_id: ""
issues:
  # ---------------------------------------------------------------------------
  # phase/1-ci-setup
  # ---------------------------------------------------------------------------
  - id: lb-0001
    title: "Set up GitHub Actions matrix workflow skeleton"
    phase: phase/1-ci-setup
    component: component/ci
    status: status/backlog
    source:
      doc: "Write out a design doc for the project.md"
      section: "§2.1"
    goal: |
      Create the GitHub Actions workflow skeleton that runs llama-builds'
      cross-OS / cross-arch build matrix. The skeleton ships with no real
      matrix entries yet; it just demonstrates that pull_request and
      workflow_dispatch triggers fire and that the job graph expands
      per-target from `targets/*/build.sh`.
    acceptance:
      - ".github/workflows/matrix.yml is committed and parses cleanly."
      - "Triggers on pull_request and workflow_dispatch."
      - "A placeholder job runs on ubuntu-latest and prints the target list."
    out_of_scope:
      - "Real CUDA / ROCm / Vulkan matrix entries (lb-0002)."
      - "Manifest scraping (lb-0003)."
    depends_on: []

  - id: lb-0002
    title: "Define empty CUDA + ROCm + Vulkan matrix entries"
    phase: phase/1-ci-setup
    component: component/ci
    status: status/backlog
    source:
      doc: "Write out a design doc for the project.md"
      section: "§2.2"
    goal: |
      Add empty CUDA, ROCm, and Vulkan entries to the matrix workflow so
      that audit_matrix.py has something to validate against. Each entry
      resolves to the right self-hosted runner label but performs no
      useful work yet.
    acceptance:
      - "matrix.yml declares cuda, rocm, and vulkan strategy entries."
      - "Each entry references a runner label that exists in the org."
      - "audit_matrix.py (lb-0005) validates these entries without error."
    out_of_scope:
      - "Actual GPU builds."
      - "Cache configuration for runners."
    depends_on:
      - lb-0001

  - id: lb-0003
    title: "Add generate_manifest.py to scrape targets/*/build.sh"
    phase: phase/1-ci-setup
    component: component/manifest
    status: status/backlog
    source:
      doc: "Write out a design doc for the project.md"
      section: "§2.3"
    goal: |
      Ship a Python script that walks targets/*/build.sh, extracts the
      METADATA block (name, repo, ref, backend, capabilities), and emits
      manifest.json conforming to the documented schema. The script is
      runnable locally and from CI.
    acceptance:
      - "scripts/generate_manifest.py exists and is executable via `python -m scripts.generate_manifest`."
      - "Emits a manifest.json that validates against schemas/manifest.schema.json."
      - "Covers at least the empty CUDA, ROCm, and Vulkan placeholder targets."
    out_of_scope:
      - "Publishing manifest.json to GitHub Pages (lb-0004)."
      - "Differential updates vs. the previous manifest."
    depends_on:
      - lb-0001

  - id: lb-0004
    title: "Publish manifest.json to GitHub Pages on every release"
    phase: phase/1-ci-setup
    component: component/manifest
    status: status/backlog
    source:
      doc: "Write out a design doc for the project.md"
      section: "§2.3"
    goal: |
      Add a release workflow that builds manifest.json (via lb-0003) and
      publishes it to the gh-pages branch so that heretek-manager's
      /api/registry endpoint has a stable URL to fetch.
    acceptance:
      - ".github/workflows/release.yml runs on tag push."
      - "manifest.json is uploaded as a Pages artifact."
      - "GET /manifest.json from the Pages URL returns the latest manifest."
    out_of_scope:
      - "Versioning the manifest (e.g., /v1/manifest.json)."
      - "Authentication on the Pages URL."
    depends_on:
      - lb-0003

  - id: lb-0005
    title: "Add audit_matrix.py pre-build validation"
    phase: phase/1-ci-setup
    component: component/ci
    status: status/backlog
    source:
      doc: "Write out a design doc for the project.md"
      section: "§2.2"
    goal: |
      Ship a Python validator that runs in CI before each matrix build
      and asserts: every matrix entry references a real runner label, every
      target's build.sh declares the metadata contract, and there are no
      orphan targets not covered by any matrix entry.
    acceptance:
      - "scripts/audit_matrix.py exists and exits non-zero on any contract violation."
      - "CI runs audit_matrix.py as the first step of every matrix build."
      - "Audit failures block the rest of the job graph from running."
    out_of_scope:
      - "Catching GPU-driver or hardware-level failures."
      - "Reporting audit results back to the GitHub UI (only stderr is required)."
    depends_on:
      - lb-0002

  - id: lb-0006
    title: "Wire GITLEAKS + super-linter + SonarCloud + pre-commit CI checks"
    phase: phase/1-ci-setup
    component: component/infra
    status: status/backlog
    source:
      doc: "Write out a design doc for the project.md"
      section: "§5.1"
    goal: |
      Add the umbrella's static-analysis and secrets-scrubbing checks to
      the llama-builds CI pipeline. The same baseline (Gitleaks,
      super-linter, SonarCloud, pre-commit) is already wired into the
      harness; this task ports those checks to the child repo.
    acceptance:
      - ".github/workflows/ci.yml runs gitleaks, super-linter, sonar, and pre-commit."
      - "Required status checks on `main` include all four."
      - "A failing secret or lint blocks the merge."
    out_of_scope:
      - "Custom SonarCloud quality gate tuning."
      - "Replacing super-linter with a lighter linter."
    depends_on:
      - lb-0001

  # ---------------------------------------------------------------------------
  # phase/4-matrix-pkg
  # ---------------------------------------------------------------------------
  - id: lb-0010
    title: "Package ggml-org/llama.cpp upstream CPU baseline"
    phase: phase/4-matrix-pkg
    component: component/upstream-sync
    status: status/backlog
    source:
      doc: "Llama Ecosystem Repository Analysis.md"
      section: "§Core"
    goal: |
      Add a `targets/upstream-cpu/` packaging entry that tracks
      ggml-org/llama.cpp main branch, builds the upstream CPU baseline,
      and ships it as the default llama-builds artifact.
    acceptance:
      - "targets/upstream-cpu/build.sh runs cleanly on a CPU runner."
      - "Generated manifest entry has backend=cpu, capabilities=[chat,embed]."
      - "Artifact is uploaded as part of the matrix workflow."
    out_of_scope:
      - "CUDA / ROCm / Vulkan specializations (lb-0011, lb-0012)."
      - "Tracking forks (lb-0013 onward)."
    depends_on: []

  - id: lb-0011
    title: "Package ggml-org/llama.cpp upstream CUDA (sm_89/90a)"
    phase: phase/4-matrix-pkg
    component: component/upstream-sync
    status: status/backlog
    source:
      doc: "Llama Ecosystem Repository Analysis.md"
      section: "§2.2"
    goal: |
      Add a CUDA matrix entry that builds llama.cpp with sm_89 and sm_90a
      CUDA architectures enabled. The resulting artifact must run on
      Ada-Lovelace and consumer-Hopper cards.
    acceptance:
      - "targets/upstream-cuda/build.sh builds against CUDA 12.x."
      - "Generated manifest entry has backend=cuda, capabilities=[chat,embed,flash-attn]."
      - "Artifact passes a smoke-test prompt on a hosted GPU runner."
    out_of_scope:
      - "sm_120 / Blackwell support (out of matrix scope)."
      - "Trellis + FlashMLA flags (lb-0013)."
    depends_on: []

  - id: lb-0012
    title: "Package ggml-org/llama.cpp upstream Vulkan"
    phase: phase/4-matrix-pkg
    component: component/upstream-sync
    status: status/backlog
    source:
      doc: "Llama Ecosystem Repository Analysis.md"
      section: "§2.2"
    goal: |
      Add a Vulkan matrix entry that builds llama.cpp against the Vulkan
      SDK. The artifact must run on any RADV-capable GPU.
    acceptance:
      - "targets/upstream-vulkan/build.sh builds against Vulkan 1.3."
      - "Generated manifest entry has backend=vulkan, capabilities=[chat,embed]."
      - "Artifact passes a smoke-test prompt via RADV."
    out_of_scope:
      - "ROCm / HIP support (separate matrix entry)."
      - "Quantization variants."
    depends_on: []

  - id: lb-0013
    title: "Package ikawrakow/ik_llama.cpp with Trellis + FlashMLA flags"
    phase: phase/4-matrix-pkg
    component: component/upstream-sync
    status: status/backlog
    source:
      doc: "Llama Ecosystem Repository Analysis.md"
      section: "§Core"
    goal: |
      Add a matrix entry that tracks ikawrakow/ik_llama.cpp and enables
      the Trellis quantization + FlashMLA attention kernels. This fork is
      the high-throughput reference for sm_89 / sm_90a cards.
    acceptance:
      - "targets/ik-llama-cpp/build.sh clones the fork at a pinned SHA."
      - "Generated manifest entry has backend=cuda, capabilities=[chat,trellis,flashmla]."
      - "Artifact demonstrates ≥1.2× throughput vs. upstream CUDA on a benchmark prompt."
    out_of_scope:
      - "Generic CPU / Vulkan builds of this fork."
      - "Multi-GPU scaling benchmarks."
    depends_on:
      - lb-0011

  - id: lb-0014
    title: "Package sgl-project/sglang with FlashInfer + Triton"
    phase: phase/4-matrix-pkg
    component: component/upstream-sync
    status: status/backlog
    source:
      doc: "Llama Ecosystem Repository Analysis.md"
      section: "§Core"
    goal: |
      Add a matrix entry that tracks sgl-project/sglang, a serving stack
      built on llama.cpp with FlashInfer attention and a Triton
      prefill/decode scheduler.
    acceptance:
      - "targets/sglang/build.sh produces a runnable OpenAI-compatible server binary."
      - "Generated manifest entry has backend=cuda, capabilities=[chat,serve,flashinfer,triton]."
      - "Server passes the llama.cpp OpenAI compat smoke test."
    out_of_scope:
      - "RadixAttention tree cache tuning."
      - "Multi-node serving."
    depends_on: []

  - id: lb-0015
    title: "Package fewtarius/CachyLLama with SSD-backed KV cache"
    phase: phase/4-matrix-pkg
    component: component/upstream-sync
    status: status/backlog
    source:
      doc: "Llama Ecosystem Repository Analysis.md"
      section: "§Core"
    goal: |
      Add a matrix entry that tracks fewtarius/CachyLLama, which adds an
      SSD-backed KV cache to llama.cpp for long-context workloads.
    acceptance:
      - "targets/cachyllama/build.sh clones the fork at a pinned SHA."
      - "Generated manifest entry has backend=cpu|cuda, capabilities=[chat,ssd-kv-cache]."
      - "Smoke test reads/writes the SSD KV cache at least once."
    out_of_scope:
      - "Distributed KV cache."
      - "CPU-only variant benchmarks."
    depends_on: []

  - id: lb-0016
    title: "Package croll83/llama.cpp-dgx with DFlash + NVFP4"
    phase: phase/4-matrix-pkg
    component: component/upstream-sync
    status: status/backlog
    source:
      doc: "Llama Ecosystem Repository Analysis.md"
      section: "§Core"
    goal: |
      Add a matrix entry that tracks croll83/llama.cpp-dgx, an
      DGX-tuned fork with the DFlash attention path and NVFP4 weight
      quantization.
    acceptance:
      - "targets/llama-cpp-dgx/build.sh clones the fork at a pinned SHA."
      - "Generated manifest entry has backend=cuda, capabilities=[chat,dflash,nvfp4]."
      - "Smoke test loads a NVFP4-quantized model and runs inference."
    out_of_scope:
      - "DGX H100/B200-specific tuning beyond the upstream patches."
      - "FP8 / FP6 quantization paths."
    depends_on: []

  - id: lb-0017
    title: "Package lemonade-sdk/llamacpp-rocm for Strix Halo"
    phase: phase/4-matrix-pkg
    component: component/upstream-sync
    status: status/backlog
    source:
      doc: "Llama Ecosystem Repository Analysis.md"
      section: "§Hardware"
    goal: |
      Add a ROCm matrix entry that tracks lemonade-sdk/llamacpp-rocm,
      which is the Strix-Halo-tuned ROCm build of llama.cpp.
    acceptance:
      - "targets/lemonade-rocm/build.sh builds against ROCm 6.x."
      - "Generated manifest entry has backend=rocm, capabilities=[chat,strix-halo]."
      - "Smoke test runs on a Strix Halo APU or its emulator."
    out_of_scope:
      - "Vega / Navi targets (only Strix Halo is in scope)."
      - "CUDA backend."
    depends_on: []

  - id: lb-0018
    title: "Package TheTom/llama-cpp-turboquant with WHT + TCQ"
    phase: phase/4-matrix-pkg
    component: component/upstream-sync
    status: status/backlog
    source:
      doc: "Llama Ecosystem Repository Analysis.md"
      section: "§Quantization"
    goal: |
      Add a matrix entry that tracks TheTom/llama-cpp-turboquant, which
      adds Walsh-Hadamard Transform (WHT) and TCQ quantization to
      llama.cpp.
    acceptance:
      - "targets/llama-cpp-turboquant/build.sh clones the fork at a pinned SHA."
      - "Generated manifest entry has backend=cpu|cuda, capabilities=[chat,wht,tcq]."
      - "Smoke test loads a WHT-quantized model and runs inference."
    out_of_scope:
      - "Quantization-aware fine-tuning tooling."
      - "Custom CUDA kernels for WHT (only upstream ones)."
    depends_on: []

  - id: lb-0019
    title: "Package AtomicBot-ai/atomic-llama-cpp-turboquant"
    phase: phase/4-matrix-pkg
    component: component/upstream-sync
    status: status/backlog
    source:
      doc: "Llama Ecosystem Repository Analysis.md"
      section: "§Quantization"
    goal: |
      Add a matrix entry that tracks AtomicBot-ai/atomic-llama-cpp-turboquant,
      the AtomicBot packaging of llama.cpp + TurboQuant.
    acceptance:
      - "targets/atomic-turboquant/build.sh clones the fork at a pinned SHA."
      - "Generated manifest entry has backend=cpu|cuda, capabilities=[chat,atomic-turboquant]."
      - "Smoke test loads an atomic-turboquant-quantized model and runs inference."
    out_of_scope:
      - "Differences from upstream llama-cpp-turboquant (lb-0018)."
      - "Distributional wrappers beyond the binary."
    depends_on: []

  - id: lb-0020
    title: "Package spiritbuun/buun-llama-cpp"
    phase: phase/4-matrix-pkg
    component: component/upstream-sync
    status: status/backlog
    source:
      doc: "Llama Ecosystem Repository Analysis.md"
      section: "§Quantization"
    goal: |
      Add a matrix entry that tracks spiritbuun/buun-llama-cpp, the
      buun-family quantization-friendly fork.
    acceptance:
      - "targets/buun-llama-cpp/build.sh clones the fork at a pinned SHA."
      - "Generated manifest entry has backend=cpu|cuda, capabilities=[chat,buun-quants]."
      - "Smoke test loads a buun-quantized model and runs inference."
    out_of_scope:
      - "Tooling for converting to buun formats (only the loader is in scope)."
      - "GPU-specific kernels beyond upstream."
    depends_on: []

  - id: lb-0021
    title: "Package huawei-csl/KVarN with Hadamard + variance normalization"
    phase: phase/4-matrix-pkg
    component: component/upstream-sync
    status: status/backlog
    source:
      doc: "Llama Ecosystem Repository Analysis.md"
      section: "§Quantization"
    goal: |
      Add a matrix entry that tracks huawei-csl/KVarN, which adds
      Hadamard transforms and variance normalization on top of KV-cache
      quantization.
    acceptance:
      - "targets/kvarn/build.sh clones the fork at a pinned SHA."
      - "Generated manifest entry has backend=cpu|cuda, capabilities=[chat,kvarn]."
      - "Smoke test loads a KVarN-quantized model and runs inference."
    out_of_scope:
      - "KVVarN-specific tensor-parallel paths."
      - "Quantization-time tooling beyond what's vendored in the fork."
    depends_on: []

  - id: lb-0022
    title: "Package carlosfundora/llama.cpp-1-bit-turbo (RDNA2 gfx1030)"
    phase: phase/4-matrix-pkg
    component: component/upstream-sync
    status: status/backlog
    source:
      doc: "Llama Ecosystem Repository Analysis.md"
      section: "§Quantization"
    goal: |
      Add a matrix entry that tracks carlosfundora/llama.cpp-1-bit-turbo,
      which targets 1-bit inference on RDNA2 gfx1030 hardware.
    acceptance:
      - "targets/llama-1bit-turbo/build.sh clones the fork at a pinned SHA."
      - "Generated manifest entry has backend=vulkan, capabilities=[chat,1bit,rdna2]."
      - "Smoke test loads a 1-bit model and runs inference via Vulkan on gfx1030."
    out_of_scope:
      - "RDNA1 / RDNA3 support (only gfx1030 in scope)."
      - "CUDA backend."
    depends_on: []

  - id: lb-0023
    title: "Package artalis-io/bitnet.c (1-bit / ternary)"
    phase: phase/4-matrix-pkg
    component: component/upstream-sync
    status: status/backlog
    source:
      doc: "Llama Ecosystem Repository Analysis.md"
      section: "§Quantization"
    goal: |
      Add a matrix entry that tracks artalis-io/bitnet.c, the standalone
      1-bit / ternary inference runtime.
    acceptance:
      - "targets/bitnet-c/build.sh clones the fork at a pinned SHA."
      - "Generated manifest entry has backend=cpu, capabilities=[chat,1bit,ternary]."
      - "Smoke test loads a ternary model and runs inference on CPU."
    out_of_scope:
      - "GPU offload."
      - "Training / fine-tuning tooling."
    depends_on: []

  - id: lb-0024
    title: "Package NVIDIA-Merlin/HierarchicalKV"
    phase: phase/4-matrix-pkg
    component: component/upstream-sync
    status: status/backlog
    source:
      doc: "Llama Ecosystem Repository Analysis.md"
      section: "§Quantization"
    goal: |
      Add a matrix entry that tracks NVIDIA-Merlin/HierarchicalKV, a
      hierarchical KV-cache quantization scheme.
    acceptance:
      - "targets/hierarchical-kv/build.sh clones the fork at a pinned SHA."
      - "Generated manifest entry has backend=cuda, capabilities=[chat,hierarchical-kv]."
      - "Smoke test loads a model with hierarchical KV cache and runs long-context inference."
    out_of_scope:
      - "HierarchicalKV-aware fine-tuning."
      - "CPU / non-CUDA backends."
    depends_on: []

  - id: lb-0025
    title: "Package abetlen/llama-cpp-python with cibuildwheel matrix"
    phase: phase/4-matrix-pkg
    component: component/upstream-sync
    status: status/backlog
    source:
      doc: "Llama Ecosystem Repository Analysis.md"
      section: "§Bindings"
    goal: |
      Add a matrix entry that builds abetlen/llama-cpp-python wheels via
      cibuildwheel across the supported Python / OS / arch combinations.
      Wheels must be uploaded to the llama-builds release page.
    acceptance:
      - "targets/llama-cpp-python/build.sh invokes cibuildwheel."
      - "Generated manifest entry has backend=cpu|cuda, capabilities=[python-bindings]."
      - "Wheels for CPython 3.11 / 3.12 on linux x86_64 are uploaded."
    out_of_scope:
      - "PyPI publishing (only the llama-builds release is in scope)."
      - "macOS / Windows wheels in this iteration (linux only)."
    depends_on:
      - lb-0011

  - id: lb-0026
    title: "Package shakfu/cyllama (Cython)"
    phase: phase/4-matrix-pkg
    component: component/upstream-sync
    status: status/backlog
    source:
      doc: "Llama Ecosystem Repository Analysis.md"
      section: "§Bindings"
    goal: |
      Add a matrix entry that builds shakfu/cyllama, the Cython bindings
      for llama.cpp.
    acceptance:
      - "targets/cyllama/build.sh clones the fork at a pinned SHA."
      - "Generated manifest entry has backend=cpu, capabilities=[cython-bindings]."
      - "Wheel builds for CPython 3.11 on linux x86_64."
    out_of_scope:
      - "GPU backend coverage in cyllama (only CPU for now)."
      - "PyPI publishing."
    depends_on: []

  - id: lb-0027
    title: "Package go-skynet/go-llama.cpp / gotzmann/llama.go / hybridgroup/yzma"
    phase: phase/4-matrix-pkg
    component: component/upstream-sync
    status: status/backlog
    source:
      doc: "Llama Ecosystem Repository Analysis.md"
      section: "§Bindings"
    goal: |
      Add a matrix entry that builds the three Go bindings
      (go-skynet/go-llama.cpp, gotzmann/llama.go, hybridgroup/yzma).
    acceptance:
      - "targets/go-llama-cpp/build.sh produces a runnable `go-llama.cpp` binary."
      - "Generated manifest entry has backend=cpu|cuda, capabilities=[go-bindings]."
      - "Smoke test runs a prompt via the `go-llama.cpp` CLI."
    out_of_scope:
      - "Vendor preference between the three Go bindings (matrix picks one)."
      - "CGO-free variants beyond what yzma provides."
    depends_on: []

  - id: lb-0028
    title: "Package SciSharp/LLamaSharp (.NET)"
    phase: phase/4-matrix-pkg
    component: component/upstream-sync
    status: status/backlog
    source:
      doc: "Llama Ecosystem Repository Analysis.md"
      section: "§Bindings"
    goal: |
      Add a matrix entry that builds SciSharp/LLamaSharp, the .NET
      bindings for llama.cpp.
    acceptance:
      - "targets/llamasharp/build.sh clones the fork at a pinned SHA."
      - "Generated manifest entry has backend=cpu|cuda, capabilities=[dotnet-bindings]."
      - "NuGet package is built but not published."
    out_of_scope:
      - "NuGet.org publishing."
      - "GPU backend coverage beyond what the upstream fork supports."
    depends_on: []

  - id: lb-0029
    title: "Package Cypheros-de/Delphi11LlamaCppBindings"
    phase: phase/4-matrix-pkg
    component: component/upstream-sync
    status: status/backlog
    source:
      doc: "Llama Ecosystem Repository Analysis.md"
      section: "§Bindings"
    goal: |
      Add a matrix entry that builds Cypheros-de/Delphi11LlamaCppBindings,
      the Delphi bindings for llama.cpp.
    acceptance:
      - "targets/delphi-llama/build.sh clones the fork at a pinned SHA."
      - "Generated manifest entry has backend=cpu, capabilities=[delphi-bindings]."
      - "Smoke test runs a Delphi demo that issues a chat prompt."
    out_of_scope:
      - "ActiveX / COM wrappers."
      - "GPU backend coverage."
    depends_on: []

  - id: lb-0030
    title: "Package mgonzs13/llama_ros (ROS 2)"
    phase: phase/4-matrix-pkg
    component: component/upstream-sync
    status: status/backlog
    source:
      doc: "Llama Ecosystem Repository Analysis.md"
      section: "§Bindings"
    goal: |
      Add a matrix entry that builds mgonzs13/llama_ros, the ROS 2
      bindings for llama.cpp.
    acceptance:
      - "targets/llama-ros/build.sh clones the fork at a pinned SHA."
      - "Generated manifest entry has backend=cpu|cuda, capabilities=[ros2-bindings]."
      - "Smoke test launches the ROS 2 node and exposes a /llama/chat service."
    out_of_scope:
      - "ROS 1 bridges."
      - "Real-time scheduling guarantees."
    depends_on: []

  - id: lb-0031
    title: "Package hiyouga/LlamaFactory UI"
    phase: phase/4-matrix-pkg
    component: component/upstream-sync
    status: status/backlog
    source:
      doc: "Llama Ecosystem Repository Analysis.md"
      section: "§Frontend"
    goal: |
      Add a matrix entry that tracks hiyouga/LlamaFactory, the
      fine-tuning + chat UI for llama.cpp-based models.
    acceptance:
      - "targets/llamafactory-ui/build.sh clones the fork at a pinned SHA."
      - "Generated manifest entry has backend=cpu|cuda, capabilities=[ui,finetune]."
      - "Smoke test starts the Gradio UI and loads a sample dataset."
    out_of_scope:
      - "Distributed fine-tuning backends."
      - "Hosted model registry integration."
    depends_on: []

  - id: lb-0032
    title: "Package mostlygeek/llama-swap orchestration"
    phase: phase/4-matrix-pkg
    component: component/upstream-sync
    status: status/backlog
    source:
      doc: "Llama Ecosystem Repository Analysis.md"
      section: "§Frontend"
    goal: |
      Add a matrix entry that tracks mostlygeek/llama-swap, the model
      orchestration tool that hot-swaps llama-server instances.
    acceptance:
      - "targets/llama-swap/build.sh clones the fork at a pinned SHA."
      - "Generated manifest entry has backend=cpu|cuda, capabilities=[orchestration,swap]."
      - "Smoke test swaps two models in sequence and verifies the API surface."
    out_of_scope:
      - "Multi-host orchestration (single host only)."
      - "GPU memory pooling beyond what llama-swap exposes."
    depends_on: []

  - id: lb-0033
    title: "Package intentee/paddler orchestration"
    phase: phase/4-matrix-pkg
    component: component/upstream-sync
    status: status/backlog
    source:
      doc: "Llama Ecosystem Repository Analysis.md"
      section: "§Frontend"
    goal: |
      Add a matrix entry that tracks intentee/paddler, the
      prefill/decode-split orchestration layer for llama.cpp.
    acceptance:
      - "targets/paddler/build.sh clones the fork at a pinned SHA."
      - "Generated manifest entry has backend=cpu|cuda, capabilities=[orchestration,paddler]."
      - "Smoke test starts prefill + decode roles and verifies routing."
    out_of_scope:
      - "Kubernetes operator beyond what the upstream ships."
      - "Custom load-balancing policies."
    depends_on: []

  - id: lb-0034
    title: "Package containers/ramalama OCI runtime"
    phase: phase/4-matrix-pkg
    component: component/upstream-sync
    status: status/backlog
    source:
      doc: "Llama Ecosystem Repository Analysis.md"
      section: "§Frontend"
    goal: |
      Add a matrix entry that ships containers/ramalama, the OCI
      container image for llama.cpp.
    acceptance:
      - "targets/ramalama/build.sh produces an OCI image artifact."
      - "Generated manifest entry has backend=cpu|cuda|rocm, capabilities=[oci-image]."
      - "Smoke test runs the image with `podman run` and serves a prompt."
    out_of_scope:
      - "Multi-arch manifests beyond linux/amd64."
      - "Container signing beyond what the upstream build does."
    depends_on: []

  - id: lb-0035
    title: "Package onicai/llama_cpp_canister (Wasm)"
    phase: phase/4-matrix-pkg
    component: component/upstream-sync
    status: status/backlog
    source:
      doc: "Llama Ecosystem Repository Analysis.md"
      section: "§Frontend"
    goal: |
      Add a matrix entry that ships onicai/llama_cpp_canister, the Wasm
      canister for running llama.cpp on the Internet Computer.
    acceptance:
      - "targets/llama-cpp-canister/build.sh clones the fork at a pinned SHA."
      - "Generated manifest entry has backend=cpu, capabilities=[wasm-canister]."
      - "Smoke test builds a canister .wasm and loads a small model."
    out_of_scope:
      - "IC ledger integration."
      - "GPU offload (canister is CPU-only)."
    depends_on: []

  - id: lb-0036
    title: "Package Lychee-Technology/llama-cpp-for-strix-halo with TTM unlock"
    phase: phase/4-matrix-pkg
    component: component/upstream-sync
    status: status/backlog
    source:
      doc: "Llama Ecosystem Repository Analysis.md"
      section: "§Hardware"
    goal: |
      Add a matrix entry that tracks Lychee-Technology/llama-cpp-for-strix-halo,
      which unlocks the full TTM (Tile Transaction Memory) on AMD Strix Halo
      APUs.
    acceptance:
      - "targets/llama-cpp-strix-halo/build.sh clones the fork at a pinned SHA."
      - "Generated manifest entry has backend=rocm|vulkan, capabilities=[chat,ttm-unlock]."
      - "Smoke test runs on a Strix Halo APU and demonstrates TTM allocation."
    out_of_scope:
      - "Strix / Strix Point coverage (only Strix Halo in scope)."
      - "Windows-driver specifics (Linux only)."
    depends_on: []

  - id: lb-0037
    title: "Package GetNyrex/strix-halo-guide documentation automation"
    phase: phase/4-matrix-pkg
    component: component/upstream-sync
    status: status/backlog
    source:
      doc: "Llama Ecosystem Repository Analysis.md"
      section: "§Hardware"
    goal: |
      Add a matrix entry that ingests GetNyrex/strix-halo-guide and
      extracts the curated llama.cpp setup steps into the llama-builds
      docs site.
    acceptance:
      - "targets/strix-halo-guide/build.sh clones the fork at a pinned SHA."
      - "Generated manifest entry has backend=docs, capabilities=[strix-halo-guide]."
      - "Rendered guide is committed to docs/ecosystem/strix-halo.md."
    out_of_scope:
      - "Interactive build tooling for Strix Halo (lb-0036)."
      - "Auto-updating the guide on upstream changes (manual refresh only)."
    depends_on: []

  # ---------------------------------------------------------------------------
  # phase/meta
  # ---------------------------------------------------------------------------
  - id: lb-0090
    title: "Update umbrella README with tracking pointer to seed file"
    phase: phase/meta
    component: component/docs
    status: status/backlog
    source:
      doc: "Write out a design doc for the project.md"
      section: "§1"
    goal: |
      Add a "Tracking" section to the llama-builds README that links to
      seeds/llama-builds.yaml and explains how to re-run seed-issues.sh.
    acceptance:
      - "README.md has a Tracking section pointing at seeds/llama-builds.yaml."
      - "Section documents the idempotent re-run semantics."
    out_of_scope:
      - "Auto-syncing the README with the seed file."
      - "Per-issue back-links."
    depends_on: []

  - id: lb-0091
    title: "Add seed-issues.sh runbook to docs/superpowers/runbooks/"
    phase: phase/meta
    component: component/docs
    status: status/backlog
    source:
      doc: "Write out a design doc for the project.md"
      section: "§13"
    goal: |
      Write a runbook under docs/superpowers/runbooks/seed-issues.md
      that documents how to invoke seed-issues.sh, how to recover from
      a partial failure, and how to add a new repo slug.
    acceptance:
      - "docs/superpowers/runbooks/seed-issues.md exists and is ≥30 lines."
      - "Runbook documents the per-repo slug convention and the labels.yaml regeneration step."
    out_of_scope:
      - "A full tutorial video."
      - "Local Docker container for offline use."
    depends_on: []
