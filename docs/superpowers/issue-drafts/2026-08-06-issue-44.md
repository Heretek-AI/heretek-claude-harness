# Model-card profile catalog (`catalog/model_profiles/`)

> Phase: 3. Type: ship. Ideation approach: external-data.
> Filed: 2026-08-06. Sources verified: 2026-08-06.

## Background

Different model classes (Qwen3.6 27B, deepseek-class, etc.) have different freshness-failure profiles. A blanket enforcement policy either over-warns on capable models or under-warns on weaker ones. Model-card profiles codify per-model enforcement settings: which gates fire, at what severity, with what latency budget — so a project can pin enforcement to its actual model class.

## Research Summary

- Tianpan deprecated-API trap: catalogued per-language deprecated-API frequencies, with model-class breakdown showing weaker models emit 3-4× more deprecated patterns ([tianpan](https://tianpan.co/blog/2026-04-17-deprecated-api-trap-ai-coding-agents), verified 2026-08-06).
- arXiv 2406.09834: empirical study across 6 model classes — quantified per-class deprecated-API rates, demonstrating that profile-driven enforcement is empirically justified ([arxiv](https://arxiv.org/html/2406.09834v1), verified 2026-08-06).

## Ideation Notes

*external-data triangulation.* Two independent sources triangulated: (a) tianpan's per-language deprecated-frequency data; (b) arXiv 2406.09834's per-model-class breakdown. Combined as a profile catalog that maps each model class to its enforcement profile (which gates, which severities). Novel in heretek because it ties enforcement to model capability — connecting heretek's hooks substrate (#37, #43) to the v2-code-quality-issue-set's model-aware quality-pack track (#19).

## Scope

- `catalog/model_profiles/<model-id>.yaml` with fields: `model_id`, `family`, `size_class`, `enforcement_profile` (gate list + severities + latency budgets).
- Initial profiles: Qwen3.6 27B, deepseek-class, plus heretek's current default.
- Hooks plugin reads the active profile (from `heretek.toml`) and configures gates accordingly.
- Validation: profiles reproduce measured per-model-class behavior within ±10%.

## Out of scope

- Auto-tuning profiles from observed behavior (follow-up; defer until profiles have shipped data).
- Per-project profile overrides (future; global profiles first).
- Model-class onboarding automation (manual curation for now).

## D5 / D7 implications

- **D5:** catalog data, not a plugin. Orthogonal to #19 quality-pack; complements it.
- **D7:** profiles cite source data (tianpan, arXiv 2406.09834); vetting bar is "per-model-class data traceable to a study or heretek's own measurements."
- **D11:** N/A — catalog is data, not a versioned plugin.
- **Hooks:** hooks plugin reads the catalog; no new hook added.

## Suggested catalog.yaml entry shape

```yaml
# catalog/model_profiles/qwen3.6-27b.yaml (sketch)
model_id: qwen3.6-27b
family: qwen
size_class: small
source:
  - https://tianpan.co/blog/2026-04-17-deprecated-api-trap-ai-coding-agents
  - https://arxiv.org/html/2406.09834v1
verified_date: 2026-08-06
enforcement_profile:
  stale_dep_intercept: warn
  ast_grep_fast_gate: warn
  forbidden_pattern_severity_floor: warn
  max_per_edit_latency_ms: 200
```

## Per-item Definition of Done

- [ ] `catalog/model_profiles/` exists with ≥3 profiles
- [ ] Each profile cites source data with verified_date
- [ ] Hooks plugin reads active profile from `heretek.toml`
- [ ] Validation harness reproduces measured per-model behavior within ±10%
- [ ] Documentation: how to add a new profile (with verification checklist)

## Cross-references

- Spec `2026-08-06-freshness-enforced-coding-roadmap-design.md` §3, §6, §7 (ship window M6–9)
- Spec `docs/superpowers/specs/2026-08-05-v2-code-quality-issue-set-design.md` Issue C: quality-pack (orthogonal layer)
- Consumes: #36 freshness cache (gate configuration), #37 + #43 hooks (gate implementations)
- Companion issues: #45 lookup-gate (profile-aware), #50 external-data long-term measurement
- Research: `docs/superpowers/research/2026-08-06-freshness-enforced-coding-research.md`