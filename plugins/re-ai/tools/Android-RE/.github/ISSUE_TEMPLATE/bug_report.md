---
name: Bug report
about: Something is broken or behaves unexpectedly
title: "[bug] "
labels: bug
assignees: ""
---

## What happened

<!-- A clear, concise description of the bug. -->

## How to reproduce

<!-- Steps to reproduce the behavior:

1. Open Claude Code with the static MCP server installed.
2. Run `mcp__android-re-static__open_project` on `<apk>`.
3. Call `mcp__android-re-static__read_manifest`.
4. See error. -->

## Expected behavior

<!-- What you expected to happen. -->

## Environment

- **OS**: (e.g. Fedora 41, macOS 15.2, Ubuntu 24.04)
- **Python version**: (output of `uv run python --version`)
- **uv version**: (output of `uv --version`)
- **Android-RE version**: (tag, commit, or `0.1.0-dev`)
- **APK target SDK**: (output of `aapt dump badging <apk>`)
- **Was the APK built from source or downloaded?**: (source URL if downloaded)

## Logs

<!-- Paste the relevant output from `bin/doctor.sh`, the MCP server, and
any error stacktraces. Use a code block. -->

```text
<paste here>
```
