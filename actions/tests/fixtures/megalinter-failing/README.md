# Sample failing repo

This file intentionally has a few lint problems that MegaLinter's
markdownlint should flag under the default ruleset.

## Bad heading spacing
The following heading has trailing whitespace.


## Trailing spaces below
This line has trailing spaces.

## A code block with no language
```
echo "no language tag"
```
