# Sample passing repo

This is a small, well-formatted markdown file used by the `lint-ultimate`
action fixture. MegaLinter should report zero findings here under the
default configuration (markdownlint, yamllint, trailing-whitespace).

## Usage

```bash
echo "hello"
```
